---
import AppShell from '@/layouts/AppShell.astro';
import TaskList from '@/components/TaskList.tsx';
const user = Astro.locals.user;
---
<AppShell title="Próximos 7 días" active="upcoming" userName={user?.name}>
  <h2 class="text-2xl font-bold mt-4">Próximos 7 días</h2>
  <TaskList view="upcoming" client:load />
</AppShell>
