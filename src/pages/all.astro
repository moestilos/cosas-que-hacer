---
import AppShell from '@/layouts/AppShell.astro';
import TaskList from '@/components/TaskList.tsx';
const user = Astro.locals.user;
---
<AppShell title="Todas" active="all" userName={user?.name}>
  <h2 class="text-2xl font-bold mt-4">Todas</h2>
  <TaskList view="all" client:load />
</AppShell>
